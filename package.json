{
  "name": "playwright-webcomponents",
  "version": "1.0.0",
  "description": "Simple Playwright test for web components",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "http-server": "^14.1.1"
  }
}
